# 通用 TDD

用于有实际测试面的功能、bug 修复和行为变更。专属 skill 可以裁决 TDD 不现实的场景。

## 默认规则

- 有实际测试面时，先写能失败的测试或 reproducer。
- 验证失败原因符合预期。
- 写最小实现让测试通过。
- 再运行相关测试和必要回归。

## 例外

以下情况可以不强制 TDD，但必须先定义验证方式：

- 内核/硬件/生产现场依赖导致无法写本地失败测试。
- 纯文档、配置或一次性探针。
- 只有源码审查或构建验证面的改动。

跳过 TDD 时，说明原因和替代验证。
